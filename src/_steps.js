/*
*Steps to use firebase ...
*1 : Create a projects on console firebase google com
*2 : install firebase;
*3 : Register Web app in firebase;
*4 : Copy firebase init with config from firebase project setting into a file firebase.init.js;
*5 : export default app;
*6 : import gerAuth from firebase/auth and create auth = getAuth(app).app.js
*7 : import app firebase init.js into app.js;
*8 : turn on google from firebase(firebase > authentication > enable google sing in);
*9 : create google provider;
*10 : provider a google account;
*/